CC=gcc

CFLAGS=-Wall -O2 -ansi -lm -g -pedantic
CLANGF=-style='{BasedOnStyle: LLVM, IndentWidth: 2}'
TARGET=main

all:
	$(CC) $(CFLAGS) src/$(TARGET).c -o bin/$(TARGET).elf

clean:
	rm bin/$(TARGET).elf

clang-format:
	clang-format $(CLANGF) -i --verbose src/$(TARGET).c
